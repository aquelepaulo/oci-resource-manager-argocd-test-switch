server {
  listen 8080;
  server_name _;

  location = / {
    default_type text/plain;
    return 200 "TESTE 1\n";
  }

  location = /healthz {
    default_type text/plain;
    return 200 "ok\n";
  }
}
