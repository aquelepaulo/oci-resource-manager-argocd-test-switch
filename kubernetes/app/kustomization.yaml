apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: test-switch-nginx
    files:
      - index.html
generatorOptions:
  disableNameSuffixHash: true
