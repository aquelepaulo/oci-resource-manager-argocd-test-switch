apiVersion: v1
kind: Service
metadata:
  name: test-switch
  namespace: gitops-demo
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: test-switch
  ports:
    - name: http
      port: 80
      targetPort: 8080
